# OPC Extensions (Reference Mirror)

These are snapshots of extensions that live in the user's local
`~/.opc/extensions/` directory. They're committed here so the OPC repo
can track them atomically. Users can symlink or copy from here into
`~/.opc/extensions/<name>/` to activate.

See `../../.harness-run3/plan.md` for the design rationale.
